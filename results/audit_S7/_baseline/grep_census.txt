########## PATTERN: \\beta ##########
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:242:We derive an upper bound on the probability of missed detection and a critical ensemble size $M_{\mathrm{crit}}$ beyond which external detection fails with probability exceeding $1 - \beta$.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:270:  For a target detection guarantee $P_{\mathrm{miss}} \le 1 - \beta$, the ensemble size must not exceed:
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:272:    M_{\mathrm{crit}} = \left\lfloor \frac{\ln \beta}{\ln\!\bigl[1 - F(\tau_{\mathrm{det}}^*)\bigr]} \right\rfloor\,.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:279:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:323:Three GARCH regimes are tested: IID (autocorrelation parameter $\alpha + \beta = 0$), Cal.~A ($\alpha + \beta \approx 0.95$), Cal.~B ($\alpha + \beta \approx 0.99$), with Student-$t_7$ innovations.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:239:We derive an upper bound on the probability of missed detection and a critical ensemble size $M_{\mathrm{crit}}$ beyond which external detection fails with probability exceeding $1 - \beta$.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:267:  For a target detection guarantee $P_{\mathrm{miss}} \le 1 - \beta$, the ensemble size must not exceed:
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:269:    M_{\mathrm{crit}} = \left\lfloor \frac{\ln \beta}{\ln\!\bigl[1 - F(\tau_{\mathrm{det}}^*)\bigr]} \right\rfloor\,.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:276:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:320:Three GARCH regimes are tested: IID (autocorrelation parameter $\alpha + \beta = 0$), Cal.~A ($\alpha + \beta \approx 0.95$), Cal.~B ($\alpha + \beta \approx 0.99$), with Student-$t_7$ innovations.
README.md:274:Records, per drift magnitude and seed, the internal adaptation delay $\tau_{\mathrm{HAT}}$ of a single Hoeffding Adaptive Tree. The empirical CDF of $\tau_{\mathrm{HAT}}$ is consumed to derive the distribution-free critical ensemble size $M_{\mathrm{crit}}$. The comparison table reports $M_{\mathrm{crit}}$ at $\beta \in \{0.50, 0.05\}$; the manuscript's worked example and the "$M_{\mathrm{crit}} \le 3$" claim use $\beta = 0.50$ (the $\beta = 0.05$ column is a conservative complement and is expectedly larger). The operational quantity $P_{\mathrm{miss}}(M{=}10)$ is $\beta$-independent.
experiments/R9_mcrit/exp_R9_compute_mcrit.py:222:    ax.set_ylabel(r"Critical ensemble size $M_{\rm crit}$  ($\beta=0.50$)")

########## PATTERN: M_crit\|M_{\\rm crit}\|M_{\\mathrm{crit}}\|Mcrit\|MCRIT ##########
experiments/R9_mcrit/exp_R9_generate_data.py:4:Experiment R9 (stage 1): single-tree HAT (M=1) instrumentation feeding the worked M_crit example.
experiments/R9_mcrit/exp_R9_generate_data.py:8:critical ensemble size M_crit. Determinized with the exact R2/R6/R7 worker-level RNG locking.
experiments/R9_mcrit/exp_R9_compute_mcrit.py:34:    M_crit = floor( ln(beta) / ln(1-F) ), STRICTLY DECREASING with respect to F.
experiments/R9_mcrit/exp_R9_compute_mcrit.py:102:    print("=== START: Empirical vs Exponential M_crit (real stream data) ===")
experiments/R9_mcrit/exp_R9_compute_mcrit.py:124:          "conservative (yields an UPPER BOUND on M_crit).")
experiments/R9_mcrit/exp_R9_compute_mcrit.py:152:            F_emp_low = max(0.0, F_emp - eps_dkw)  # borne basse -> M_crit max (defensif)
experiments/R9_mcrit/exp_R9_compute_mcrit.py:159:                m_emp_dkw_up = mcrit_from_F(F_emp_low, beta)  # Lower bound -> M_crit max (defensive)
experiments/R9_mcrit/exp_R9_compute_mcrit.py:174:                    "Mcrit_emp": m_emp,
experiments/R9_mcrit/exp_R9_compute_mcrit.py:175:                    "Mcrit_exp": m_exp,
experiments/R9_mcrit/exp_R9_compute_mcrit.py:176:                    "Mcrit_alt": m_alt,
experiments/R9_mcrit/exp_R9_compute_mcrit.py:177:                    "Mcrit_emp_dkw_up": m_emp_dkw_up,
experiments/R9_mcrit/exp_R9_compute_mcrit.py:185:                  f"Mcrit(.50) emp={mcrit_from_F(F_emp,0.5)} exp={mcrit_from_F(F_exp,0.5)} | "
experiments/R9_mcrit/exp_R9_compute_mcrit.py:194:    # Figure: M_crit emp (solid) vs exp (dashed) vs Delta_e, by lambda
experiments/R9_mcrit/exp_R9_compute_mcrit.py:197:    # Justification: For Delta_e < 0.20 (Safe Zone), the detector wins, M_crit is
experiments/R9_mcrit/exp_R9_compute_mcrit.py:208:        m_emp = s["Mcrit_emp"].astype(float).values
experiments/R9_mcrit/exp_R9_compute_mcrit.py:209:        m_exp = s["Mcrit_exp"].astype(float).values
experiments/R9_mcrit/exp_R9_compute_mcrit.py:222:    ax.set_ylabel(r"Critical ensemble size $M_{\rm crit}$  ($\beta=0.50$)")
experiments/R9_mcrit/exp_R9_compute_mcrit.py:224:    ax.set_title(r"Empirical vs exponential $M_{\rm crit}$ (blind-spot regime, real $\tau_{\rm HAT}$)",
experiments/R9_mcrit/exp_R9_compute_mcrit.py:229:    fig_path = FIG_DIR / "Fig_R9_Mcrit_empirical_vs_exp.png"
experiments/R9_mcrit/exp_R9_compute_mcrit.py:253:    print("RECOMMENDATION: Distribution-free evaluation (M_crit on empirical CDF F_emp) "
README.md:273:### Experiment R9: The Critical Ensemble Size ($M_{\mathrm{crit}}$)
README.md:274:Records, per drift magnitude and seed, the internal adaptation delay $\tau_{\mathrm{HAT}}$ of a single Hoeffding Adaptive Tree. The empirical CDF of $\tau_{\mathrm{HAT}}$ is consumed to derive the distribution-free critical ensemble size $M_{\mathrm{crit}}$. The comparison table reports $M_{\mathrm{crit}}$ at $\beta \in \{0.50, 0.05\}$; the manuscript's worked example and the "$M_{\mathrm{crit}} \le 3$" claim use $\beta = 0.50$ (the $\beta = 0.05$ column is a conservative complement and is expectedly larger). The operational quantity $P_{\mathrm{miss}}(M{=}10)$ is $\beta$-independent.
README.md:283:- **Figure:** `results/R9_mcrit/figures/Fig_R9_Mcrit_empirical_vs_exp.png`
README.md:285:> **Reproducibility:** `tests/test_R9_mcrit.py` asserts that the regenerated artifact reproduces the exact numerical example of the manuscript Corollary ($M_{\mathrm{crit}}=1$).
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:75:  \item \textbf{The Blind Spot Paradox.} We formalize the race condition and trace it to internal ADWIN hyper-reactivity, whose stationary \emph{Starvation Effect} defeats CUSUM and is amplified by the ensemble-level \emph{Hydra Effect}. We derive the critical ensemble size $M_{\mathrm{crit}}$ inducing structural failure.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:242:We derive an upper bound on the probability of missed detection and a critical ensemble size $M_{\mathrm{crit}}$ beyond which external detection fails with probability exceeding $1 - \beta$.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:272:    M_{\mathrm{crit}} = \left\lfloor \frac{\ln \beta}{\ln\!\bigl[1 - F(\tau_{\mathrm{det}}^*)\bigr]} \right\rfloor\,.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:274:  For $M > M_{\mathrm{crit}}$ this (conservative) certificate no longer holds, and reliability must be settled empirically rather than inferred from the bound.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:278:Corollary~\ref{cor:mcrit} requires the scalar $F(\tau_{\mathrm{det}}^*)$. A Kolmogorov--Smirnov test ($N{=}2000$ bootstrap) rejects an exponential fit of the single-tree delays $\tau_{\mathrm{HAT}}$ at every tested magnitude ($p<0.05$), so we evaluate $M_{\mathrm{crit}}$ directly from the empirical CDF $\widehat{F}$ of $\tau_{\mathrm{HAT}}$.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:279:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:428:We formalized the \textbf{blind spot paradox}: adaptive classifiers systematically defeat external \emph{cumulative-evidence} drift detectors by absorbing distributional changes before the accumulator reaches its alarm threshold. We traced it to one root cause---internal ADWIN hyper-reactivity---whose stationary \emph{Starvation Effect} is amplified by the ensemble-level \emph{Hydra Effect}; we derived the critical size $M_{\mathrm{crit}}$ beyond which cumulative monitors structurally fail. Windowed and distributional monitors are spared by construction.
tests/test_R9_mcrit.py:3:Non-regression test for Experiment R9 (worked M_crit example, distribution-free).
tests/test_R9_mcrit.py:17:    # The worked example and the "M_crit <= 3" claim are stated at beta = 0.50. The CSV also
tests/test_R9_mcrit.py:18:    # tabulates a conservative beta = 0.05 (necessarily larger M_crit), which is NOT the claim.
tests/test_R9_mcrit.py:24:    assert int(row["Mcrit_emp"]) == 1, row["Mcrit_emp"]
tests/test_R9_mcrit.py:29:    assert grid["Mcrit_emp"].max() <= 3, grid["Mcrit_emp"].max()
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:75:  \item \textbf{The Blind Spot Paradox.} We formalize the race condition and trace it to internal ADWIN hyper-reactivity, whose stationary \emph{Starvation Effect} defeats CUSUM and is amplified by the ensemble-level \emph{Hydra Effect}. We derive the critical ensemble size $M_{\mathrm{crit}}$ inducing structural failure.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:239:We derive an upper bound on the probability of missed detection and a critical ensemble size $M_{\mathrm{crit}}$ beyond which external detection fails with probability exceeding $1 - \beta$.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:269:    M_{\mathrm{crit}} = \left\lfloor \frac{\ln \beta}{\ln\!\bigl[1 - F(\tau_{\mathrm{det}}^*)\bigr]} \right\rfloor\,.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:271:  For $M > M_{\mathrm{crit}}$ this (conservative) certificate no longer holds, and reliability must be settled empirically rather than inferred from the bound.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:275:Corollary~\ref{cor:mcrit} requires the scalar $F(\tau_{\mathrm{det}}^*)$. A Kolmogorov--Smirnov test ($N{=}2000$ bootstrap) rejects an exponential fit of the single-tree delays $\tau_{\mathrm{HAT}}$ at every tested magnitude ($p<0.05$), so we evaluate $M_{\mathrm{crit}}$ directly from the empirical CDF $\widehat{F}$ of $\tau_{\mathrm{HAT}}$.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:276:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:425:We formalized the \textbf{blind spot paradox}: adaptive classifiers systematically defeat external \emph{cumulative-evidence} drift detectors by absorbing distributional changes before the accumulator reaches its alarm threshold. We traced it to one root cause---internal ADWIN hyper-reactivity---whose stationary \emph{Starvation Effect} is amplified by the ensemble-level \emph{Hydra Effect}; we derived the critical size $M_{\mathrm{crit}}$ beyond which cumulative monitors structurally fail. Windowed and distributional monitors are spared by construction.

########## PATTERN: 93 ##########
experiments/R3_regime_crossover/exp_R3_regime_crossover.py:63:    # AE Visual Match: Reverting to Legacy RandomState (MT19937) instead of PCG64
docs/manuscript/tables/table1_proteus_summary.tex:3:  \caption{Concept Drift detection on heteroscedastic ProteuS streams with the static RF resolution, all pipelines generated by a SINGLE aligned method (12 transitions $\times$ 30 seeds $=$ 360 runs per cell; identical streams, seeds, and ARF configuration). Mean $\pm$ 95\% CI (1000 bootstrap resamples); $\infty$ denotes detection failure. Significance is assessed at the seed level (the unit of statistical independence of the synthetic generator): PHT+ARF c=1 detects 0/1080 runs vs 932/1080 for PHT+HT; EDDM+ARF c=1 detects 0/1080 runs vs 882/1080 for EDDM+HT; SRP+PHT c=1 detects 0/1080 runs vs 932/1080 for PHT+HT; KSWIN+ARF c=1 detects 1080/1080 runs vs 0/1080 for PHT+ARF c=1. All 30 seeds separate completely (paired sign test $p \approx 2\times10^{-9}$); Wilcoxon is deprecated. KSWIN ADD includes a structural lag $W/2 = 15$; bracketed $[X]$ is lag-corrected. Boldface marks the blind-spot collapse (F1=0.00) and its KSWIN resolution (F1=1.00).}
experiments/R5_real_world_evaluation/exp_R5_config.py:51:# ~79,986 instances: positions 79932 (truncated post-window) and 106497
experiments/R5_real_world_evaluation/exp_R5_config.py:56:    "incremental_reoccurring_balanced": [26568, 53364, 79932, 106497],          # K=4 raw
README.md:29:*   **INSECTS Dataset:** The `abrupt_balanced.csv`, `gradual_balanced.csv`, and `incremental_reoccurring_balanced.csv` streams are sourced from the official [INSECTS repository](https://drive.google.com/drive/folders/1v-iRL6X4yWhKIn82-eS-w93wb-gxjSiz).
README.md:227:> The Section IV-C flooding discussion in the submitted manuscript quotes **93 alarms** (precision $0.011$) for the `PHT+ARF(c=1)` pipeline on the `gradual_balanced` stream, whereas this artifact and the finalized Table II report the reproducible value of **86 alarms** (precision $0.012$). The discrepancy is a stale in-line figure in the manuscript text: the Table II body and all conclusions were finalized against this pipeline's deterministic output, but the alarm count quoted in the prose was not updated in lockstep. The repository value (**86 alarms**, reproduced bit-for-bit on every run) is authoritative. The scientific conclusion — massive false-alarm flooding by `PHT+ARF(c=1)` against only **7 alarms** for the `PHT+HT` baseline — is entirely unchanged.
experiments/R4_proteus_evaluation/exp_R4_main_table.py:60:    'SPY': {'mu': 0.112/252, 'omega': 1.2e-7, 'alpha': 0.09,  'beta': 0.893},
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:94:\noindent\textbf{The PHT}~\cite{page_1954} is a CUSUM detector~\cite{basseville_1993}: an alarm fires when the running maximum of its accumulated statistic exceeds a threshold $\lambda > 0$ (governing the false-alarm rate~\cite{tartakovsky_2014}), net of a minimum-magnitude tolerance $\delta_P > 0$. By Wald's equation, the expected accumulation time after an abrupt mean shift $\Delta\mu$ is $\tau_{\mathrm{det}} = \lambda / (\Delta\mu - \delta_P) = \mathcal{O}(1/\Delta e)$, where $\Delta e$ denotes the effective error jump.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:357:When testing the Streaming Random Patches (SRP)~\cite{gomes_srp_2019} ensemble paired with a PHT, we observe the exact same phenomenon: F1${=}0.00$ at $c_{\mathrm{int}}=1$ ($0/1080$ runs detected vs.\ $932/1080$ for PHT$+$HT; complete separation across all 30 seeds). This definitively confirms Corollary~\ref{cor:mcrit}: any bagging-based ensemble satisfying an adaptation time $\tau_{\mathrm{ARF}} = \mathcal{O}((\Delta e)^{-\alpha})$ with $\alpha \approx 1$ will structurally starve a cumulative-evidence external monitor.
experiments/R4_proteus_evaluation/exp_R4_kswin_sweep.py:51:    'SPY': {'mu': 0.112/252, 'omega': 1.2e-7, 'alpha': 0.09,  'beta': 0.893},
results/R4_proteus_evaluation/tables/table1_proteus_summary.tex:3:  \caption{Concept Drift detection on heteroscedastic ProteuS streams with the static RF resolution, all pipelines generated by a SINGLE aligned method (12 transitions $\times$ 30 seeds $=$ 360 runs per cell; identical streams, seeds, and ARF configuration). Mean $\pm$ 95\% CI (1000 bootstrap resamples); $\infty$ denotes detection failure. Significance is assessed at the seed level (the unit of statistical independence of the synthetic generator): PHT+ARF c=1 detects 0/1080 runs vs 932/1080 for PHT+HT; EDDM+ARF c=1 detects 0/1080 runs vs 882/1080 for EDDM+HT; SRP+PHT c=1 detects 0/1080 runs vs 932/1080 for PHT+HT; KSWIN+ARF c=1 detects 1080/1080 runs vs 0/1080 for PHT+ARF c=1. All 30 seeds separate completely (paired sign test $p \approx 2\times10^{-9}$); Wilcoxon is deprecated. KSWIN ADD includes a structural lag $W/2 = 15$; bracketed $[X]$ is lag-corrected. Boldface marks the blind-spot collapse (F1=0.00) and its KSWIN resolution (F1=1.00).}
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:94:\noindent\textbf{The PHT}~\cite{page_1954} is a CUSUM detector~\cite{basseville_1993}: an alarm fires when the running maximum of its accumulated statistic exceeds a threshold $\lambda > 0$ (governing the false-alarm rate~\cite{tartakovsky_2014}), net of a minimum-magnitude tolerance $\delta_P > 0$. By Wald's equation, the expected accumulation time after an abrupt mean shift $\Delta\mu$ is $\tau_{\mathrm{det}} = \lambda / (\Delta\mu - \delta_P) = \mathcal{O}(1/\Delta e)$, where $\Delta e$ denotes the effective error jump.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:354:When testing the Streaming Random Patches (SRP)~\cite{gomes_srp_2019} ensemble paired with a PHT, we observe the exact same phenomenon: F1${=}0.00$ at $c_{\mathrm{int}}=1$ ($0/1080$ runs detected vs.\ $932/1080$ for PHT$+$HT; complete separation across all 30 seeds). This definitively confirms Corollary~\ref{cor:mcrit}: any bagging-based ensemble satisfying an adaptation time $\tau_{\mathrm{ARF}} = \mathcal{O}((\Delta e)^{-\alpha})$ with $\alpha \approx 1$ will structurally starve a cumulative-evidence external monitor.

########## PATTERN: reliability\|RELIABILITY\|Reliability ##########
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:274:  For $M > M_{\mathrm{crit}}$ this (conservative) certificate no longer holds, and reliability must be settled empirically rather than inferred from the bound.
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:279:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:294:  \emph{(ii)~Operational calibration.} Condition~\eqref{eq:decoupling} is existential and circular as stated. The following procedure renders it constructive over an operational envelope $[\Delta e_{\min}, \Delta e_{\max}]$ of drift magnitudes to be guaranteed at reliability $1-\alpha$:
docs/manuscript/articleA_blindspot_v64_camera_ready.tex:430:To restore reliability, we introduced the \textbf{Decoupling Principle}: external monitors must be strictly more reactive than internal classifiers. Our empirical validation exposes a Fundamental Tension on heteroscedastic streams, where reliable decoupling and false-alarm control are mutually exclusive under CUSUM architectures. Consequently, the principal operational resolution is detector-side: substituting cumulative-evidence monitors with distribution-based tests like KSWIN escapes the starvation regime entirely, restoring perfect detection without sacrificing the adaptive ensemble's predictive power.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:271:  For $M > M_{\mathrm{crit}}$ this (conservative) certificate no longer holds, and reliability must be settled empirically rather than inferred from the bound.
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:276:At $\Delta e = 0.33$ ($\lambda=50$), instrumentation yields $\mathbb{E}[\tau_{\mathrm{HAT}}] = 463$, $\tau_{\mathrm{det}}^* = 155$, and $\widehat{F}(\tau_{\mathrm{det}}^*) = 0.49$, so the (conservative) independence bound~\eqref{eq:mcrit} yields $M_{\mathrm{crit}} = 0$ at the reliability target $\beta = 0.95$---it cannot certify reliable detection even for a single tree, a verdict that persists across the operative grid ($\Delta e \ge 0.24$).
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:291:  \emph{(ii)~Operational calibration.} Condition~\eqref{eq:decoupling} is existential and circular as stated. The following procedure renders it constructive over an operational envelope $[\Delta e_{\min}, \Delta e_{\max}]$ of drift magnitudes to be guaranteed at reliability $1-\alpha$:
docs/manuscript/articleA_blindspot_v63_camera_ready.tex:427:To restore reliability, we introduced the \textbf{Decoupling Principle}: external monitors must be strictly more reactive than internal classifiers. Our empirical validation exposes a Fundamental Tension on heteroscedastic streams, where reliable decoupling and false-alarm control are mutually exclusive under CUSUM architectures. Consequently, the principal operational resolution is detector-side: substituting cumulative-evidence monitors with distribution-based tests like KSWIN escapes the starvation regime entirely, restoring perfect detection without sacrificing the adaptive ensemble's predictive power.

